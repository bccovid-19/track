---
fields:
  batched:
    - name: Face Shield Frame
      openproject_key: customField7
      batch_size: 10
    - name: Visors
      openproject_key: customField8
      batch_size: 10
    - name: Ear Savers
      openproject_key: customField9
      batch_size: 10

server:
  port: 80
  base_url: /api/v1

openproject:
  url: {{ scheme }}://{{ apps.openproject.hostname }}
  username: {{ openprojectApi.username }}
  password: {{ openprojectApi.password }}
  apikey: {{ openprojectApi.apikey }}
